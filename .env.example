# Optional: YouTube Data API v3 key for "Learn with Purpose" playlist.
# Enable YouTube Data API v3 in Google Cloud. The playlist is loaded at build/dev time (server)
# and, if needed, in the browser. For localhost: add http://localhost/* and http://127.0.0.1/*
# to the key's HTTP referrer list in Google Cloud Console so videos load in dev.
# PUBLIC_YOUTUBE_API_KEY=

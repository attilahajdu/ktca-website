# Optional: YouTube Data API v3 key for "Learn with Purpose" playlist (client-side fetch on page load).
# Restrict the key by HTTP referrer to your site domain. Enable YouTube Data API v3 in Google Cloud.
# PUBLIC_YOUTUBE_API_KEY=
